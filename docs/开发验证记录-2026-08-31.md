# v2 开发与验证记录

## 本次实现

- 全员×日期有效考勤底表；当前部门归属，区分未打卡占位与真实动作。
- 0工时与缺失分开；保留多段班次，固定UTC+8，跨夜归属日幂等处理。
- 加班采用19:00之后有效到离岗时段，独立于官方审批/计薪加班字段。
- 企业微信当日规则、个人排班表、补卡列表和详情接口；保留审批状态与当前有效结果。
- 补卡分页与状态筛选；全员数据覆盖查询，包含没有打卡记录的人员。
- 当前月回补；已知历史待审批单重新查询状态，历史日报未被本次刷新时仍标待同步。
- 逐人员/日期/接口成功分片替换；失败与跳过部分保留；原子JSON/CSV、上一版快照及跨进程抓取锁。
- 统一比率分母及等长环比；未知不填0；移除加班越多越好的综合评分。
- HTML与CSV安全处理、请求参数校验、同源抓取、loopback Host校验和CSP。
- 导出按当前日期/部门/规则；地图使用有效异常和企业微信真实规则半径。
- 地点热力配色改为高饱和蓝→青→绿→黄→橙→红色带，热核满饱和不透明，取代原先被浅色底图冲淡的低饱和半透明样式；页面临界色带与回归测试同步更新。

## 真实接口与数据

白名单恢复后已成功调用企业微信只读接口。最新源快照生成于2026-08-31 21:33:26。
本次完整刷新2026-08-01至2026-08-30，保留此前2026-07-30至07-31的1102条原始历史记录。
正式JSON合并后：281名人员、12388条记录、7545条日报、4800条逐日规则、180个补卡卡位。
180个卡位中168通过、9待审批、2驳回、1撤回；卡位不是独立申请单数，页面还会按考勤归属日期筛选。

## 验证结果

- Python：78项通过，0跳过；原来的本机配置导致跳过的凭证测试改用隔离模拟。
- 原指标测试：153项通过。
- 新业务反例：24项通过。
- 图表构造冒烟：38个图表、空筛选和tooltip转义通过；这不是像素级截图测试。
- 浏览器：总览、时间、加班、部门、全员覆盖、补卡分页/待审批筛选、深色模式及抓取抽屉检查通过；未提交浏览器抓取任务。
- 地点热力：浏览器实际截图确认新标准热力配色的饱和热核与橙/黄/青/蓝渐层，密度分层清晰、可见性明显提升。
- API导出：日期及部门筛选匹配；不可信Host返回403。
- 正式服务已切换回127.0.0.1:8787，保留loopback监听。
- 地图有效异常使用独立反例测试，未对真实地理点位进行外部底图截图QA。

## 数据缺口与边界

默认2026-08-01至08-30范围中，48人至少有一天缺少日报或可核定排班，共1351个人日。
这些人仍计入全员并可查询；总体出勤/迟到/早退/缺卡/准点率不伪造，暂显示待核定。
需要在企业微信核对人员规则和应用可见范围。其他已取得证据的工时和异常计数仍可查看。
默认不包含尚未结束的当日；如明确查看当日可在抓取日期中选择当天，但结果是未结束日快照。
这是管理层本地应用，不是公网托管部署；未增加定时任务或分角色登录。

## 回退材料

- 修改前源码与数据：`/Users/ljp/Desktop/测试/wecom-checkin-before-fixes.WPIpcN/source-and-data.tar.gz`（不含凭证文件）。
- 上一版正式数据：`data/checkin_data.json.previous`。
- 真实API暂存与同步状态：`/Users/ljp/Desktop/测试/wecom-checkin-stage.ts8jQv/`。
- 本目录不是Git仓库，本轮未执行Git初始化或提交。

验证命令：`bash tests/run.sh`。指标口径详见同目录《指标体系.md》。

## 后续范围调整

按用户列出的四个部门排除其当前成员：其他人员36人、辅助账号6人、董事长1人、总经办4人，共47人。
原始通讯录仍为281人，当前应考勤分析范围为234人。服务端人员、记录、日报、月报、规则、补卡及HTTP/本地CSV统一应用排除范围；按任一当前部门精确匹配，调岗人员不按旧部门误排除。
新增4项范围回归测试，全套Python82项、原指标153项、业务反例24项及38图表构造检查通过；最后的当前部门映射校验通过。
HTTP接口已验证234人、排除47人，CSV范围已核对。原始JSON校验一致、未删除数据。自动浏览器刷新遇到浏览器安全策略阻断，需用户手动重新打开看板。

## 缺口不再阻断全部比率

取消“任何未知人日都会隐藏总体比率”的门槛。出勤、准点、迟到、早退、缺卡及异常频率统一以已知工作日且有官方日报的人日作为分子、分母范围；无核定分母仍为空。
人员底表保留所有234人及未知日期，不将首次出现记录当入职日期。新增核定分母、缺口人数、人日数与依据覆盖率；已明确休息日算有依据，未知排班和缺日报重叠不重复计数。
2026-08-01至08-30快照：核定出勤4034/4580=88.1%；依据覆盖6869/7020=97.8%；仍有8人151个人日未参与比率。当前值照常显示，覆盖不齐的首尾两期不做优劣环比。
此次未重抓企业微信数据、未改原始JSON。新增6项反例覆盖分子分母同口径、休息日、重叠缺口、月初缺数据、全未知与环比防误读；业务反例累计30项通过。
全套Python82项、指标153项及38个图表构造检查通过。浏览器已验证总览显示88.1%和97.8%，无脚本错误；同时调整KPI说明换行，避免覆盖提示把明细挤成竖排。

## 地图展示与缩放调整

- 默认“柔和热力”：浮点累加、可分离高斯平滑、连续透明度，不再叠加实心红色锚点，避免大量重叠后的硬色块。
- 新增“蓝色密度”，保留点位气泡与地点聚合；柔化滑块仅调整视觉扩散。图例明确当前视野相对密度，不改变打卡记录或指标。
- 删除“企业微信规则范围”选项和范围圈渲染，后台排班规则仍保留。
- 滚轮/触摸板累计达到阈值才变化一级，连续触发设置240ms冷却，以鼠标为缩放锚点。离线与在线保持相同投影，避免离线缩放失效。
- 16项地图测试覆盖透明度不饱和、边缘渐隐、视野边缘、单色、柔化、滚轮/捏合、实际事件绑定、锚点与绘制缓存。使用合成点位生成本地视觉预览并检查，不作为真实员工数据。
- 10000个合成点位的单次密度及颜色计算约22ms，仅为本机算法观测，不是浏览器帧率承诺。
- 在线底图刷新/缩放复测等待用户确认；未在未确认时触发新的在线地图请求。
- 修改前地图源码备份：`/Users/ljp/Desktop/测试/wecom-map-before-fix.mDK9ky/map-source.tar.gz`。
